<!DOCTYPE html>
<html lang="en">
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta charset="UTF-8">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
    <link rel="stylesheet" href="/css/index.css?v=1.1">
    <title>{{ actionName }}</title>
</head>

<body>
    <div class="header">
        <a href="/">
            <div class="logo">
                <h1>Planetarium <i class="material-icons">rocket_launch</i></h1>
            </div>
        </a>
        <div class="panel panelRight">
            <div class="profile">
                <a href="/" class="linkButton smallLink">
                    <i class="material-icons">home</i>
                    <p>Strona główna</p>
                </a>
                <a href="/user" class="linkButton smallLink">
                    <i class="material-icons">person</i>
                    <p>Twój Profil</p>
                </a>
                <a href="/logout" class="linkButton smallLink">
                    <i class="material-icons">logout</i>
                    <p>Wyloguj się</p>
                </a>
            </div>
        </div>
    </div>

    <div class="panel">
        <div class="adminPanel panelRight">
            <a href="/positions" class="linkButton smallLink">
                <i class="material-icons">arrow_back</i>
                <p>Wróć do zawodów</p>
            </a>
        </div>
    </div>

    <div>
        <form method="post" action="{{ actionLink }}" class="centerForm" style="grid-template-columns: repeat(1, minmax(200px, 1fr)); width: 400px">
            <h3 class="fullSpan">{{ formName }}</h3>

            <div class="fullSpan">                
                {% for error in errors %}
                    <h5 class="bg">{{ error }}</h5>
                {% endfor %}
            </div>
            <div class="formPart">
                <h5>Dane Zawodu</h5>

                {% for key, value in keys %}
                    <label for="{{ value }}">{{ key }}</label>
                    <input name="{{ value }}" id="{{ value }}" value="{{ positionData[key] }}">
                {% endfor %}
            </div>

            <input type="submit" value="{{ actionName }}" class="fullSpan">
        </form>
    </div>

    <script src="/js/showLinkName.js"></script>
</body>
</html>