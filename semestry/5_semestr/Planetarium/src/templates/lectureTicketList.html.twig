<!DOCTYPE html>
<html lang="en">
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta charset="UTF-8">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
    <link rel="stylesheet" href="/css/index.css?v=1.1">
    <title>Planetarium</title>
</head>

<body>
    <div class="header">
        <a href="/">
            <div class="logo">
                <h1>Planetarium <i class="material-icons">rocket_launch</i></h1>
            </div>
        </a>
        <div class="panel panelRight">
            <div class="profile">
                <a href="/" class="linkButton smallLink">
                    <i class="material-icons">home</i>
                    <p>Strona główna</p>
                </a>
                <a href="/user" class="linkButton smallLink">
                    <i class="material-icons">person</i>
                    <p>Twój Profil</p>
                </a>
                <a href="/logout" class="linkButton smallLink">
                    <i class="material-icons">logout</i>
                    <p>Wyloguj się</p>
                </a>
            </div>
        </div>
    </div>

    <h3>Dostępne wykłady</h3>
    <div class="content grid">
        {% for lecture in lectures %}
            {% if lecture.Data != '1900-01-01 00:00:00' and lecture.Cena != '0' %}
            <div class="shortProfile largeLink">
                <h4>{{ lecture.Tytuł }}</h4>
                <h4>{{ lecture.Opis }}</h4>
                <h4>Czas trwania: {{ lecture['Czas Trwania'] }} min.</h4>
                <h4>Cena: {{ lecture.Cena }} zł</h4>
                
                {% if lecture.Dostępność == 'true' %}
                    <form action="/ticket/{{lecture.Id}}" method="post">
                        <input type="submit" value="Dodaj do biletu">
                    </form>
                {% else %}
                    <h4>Brak wolnych miejsc</h4>
                {% endif %}
            </div>
            {% endif %}
        {% endfor %}
    </div>
    
    <script src="/js/showLinkName.js"></script>
</body>
</html>